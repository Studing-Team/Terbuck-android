<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Terbuck" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/green_50</item>
        <item name="android:statusBarColor">@color/transparent</item>
        <item name="android:textColor">@color/black_50</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="colorControlActivated">@color/green_50</item> <!-- 커서 색상 -->
        <item name="android:windowLightStatusBar">true</item>   <!-- 상태바 아이콘(true: 검정 / false: 흰색) -->
    </style>

    <style name="Theme.Terbuck" parent="Base.Theme.Terbuck" />
</resources>